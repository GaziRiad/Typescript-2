let userName: string;

userName = "Max";

let userAge: number;

userAge = 22;

let isValid = true;

// string, number, boolean

let userId: string | number = "abc1";
userId = 123;

// let user: object;
// user = "Max";

let user: {
  id: string | number;
  name: string;
  age: number;
  isAdmin: boolean;
};

user = {
  id: "Abc1",
  name: "Riad Hallouch",
  age: 22,
  isAdmin: true,
};
